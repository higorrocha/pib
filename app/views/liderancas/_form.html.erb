<%= form_for @lideranca do |f| %>

	<% if @lideranca.errors.any? %>
		<h2><%= pluralize(@lideranca.errors.count, "error") %> prevented this lideranca from saving:</h2>
		<ul>
			<% @lideranca.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	<% end %>

	<%= f.label :title %><br>
	<%= f.text_field :title %>

	<br>
	<br>

	<%= f.label :content, "Escreva sobre:" %><br>
	<%= f.text_area :content %>

	<br>
	<br>

	<%= f.label :image %><br>
	<%= f.file_field :image %>

	<br>
	<br>

	<%= f.submit %>

<% end %>